[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lerobot_teleoperator_gello"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "lerobot==0.5.0",
    "dynamixel-sdk>=3.8.4",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
